<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.huadi.intelligentteachingplatform.mapper.QuestionnaireMapper">

    <resultMap id="BaseResultMap" type="com.huadi.intelligentteachingplatform.entity.Questionnaire">
        <id column="id" property="id"/>
        <result column="teacher_id" property="teacherId"/>
        <result column="class_id" property="classId"/>
        <result column="course_id" property="courseId"/>
        <result column="title" property="title"/>
        <result column="content_json" property="contentJson"/>
        <result column="status" property="status"/>
        <result column="response_count" property="responseCount"/>
        <result column="avg_score" property="avgScore"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT id, teacher_id, class_id, course_id, title, content_json, status, response_count, avg_score, create_time
        FROM tb_questionnaire
        <where>
            <if test="teacherId != null">
                teacher_id = #{teacherId}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, teacher_id, class_id, course_id, title, content_json, status, response_count, avg_score, create_time
        FROM tb_questionnaire
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.huadi.intelligentteachingplatform.entity.Questionnaire"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_questionnaire (teacher_id, class_id, course_id, title, content_json, status, response_count, avg_score)
        VALUES (#{teacherId}, #{classId}, #{courseId}, #{title}, #{contentJson}, #{status}, #{responseCount}, #{avgScore})
    </insert>

    <update id="update" parameterType="com.huadi.intelligentteachingplatform.entity.Questionnaire">
        UPDATE tb_questionnaire
        SET title = #{title},
            content_json = #{contentJson}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM tb_questionnaire WHERE id = #{id}
    </delete>

    <update id="updateStatus">
        UPDATE tb_questionnaire SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
